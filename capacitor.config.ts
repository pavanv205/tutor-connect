import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hometutor.tutorconnect',
  appName: 'Tutor Connect',
  webDir: 'dist'
};

export default config;
